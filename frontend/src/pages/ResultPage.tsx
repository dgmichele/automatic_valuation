/**
 * ResultPage — stub (Fase 1)
 * Pagina /risultato: mostra overlay di calcolo, risultato criptato e lead form.
 * Implementata completamente in Fase 4.
 */
const ResultPage = () => {
  return (
    <div className="flex-1 flex items-center justify-center">
      <p className="text-brand-paragraph font-sans text-sm">
        ResultPage — stub Fase 1
      </p>
    </div>
  );
};

export default ResultPage;
